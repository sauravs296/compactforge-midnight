#!/bin/bash
if [ "$1" == "--version" ]; then
  echo "compact version 0.16.0 (mocked for CI)"
  exit 0
fi

if [ "$1" == "compile" ]; then
  echo "Mock compiling $2 to $3..."
  echo "Compilation successful."
  exit 0
fi

echo "Mock compact CLI: Unknown command $1"
exit 1
